/*
** EPITECH PROJECT, 2019
** pushswap
** File description:
** pushswap functions
*/

/*#include "my.h"
#include "pushswap.h"

void sa(list_t *list)
{
    my_putstr(OPE_SA);
    if (list->size >= 2)
        my_swap(&list->tab[0], &list->tab[1]);
}

void sb(list_t *list)
{
    my_putstr(OPE_SB);
    if (list->size >= 2)
        my_swap(&list->tab[0], &list->tab[1]);
}

void sc(list_t *list_a, list_t *list_b)
{
    my_putstr(OPE_SC);
    if (list_a->size >= 2)
        my_swap(&list_a->tab[0], &list_a->tab[1]);
    if (list_b->size >= 2)
        my_swap(&list_b->tab[0], &list_b->tab[1]);
}*/